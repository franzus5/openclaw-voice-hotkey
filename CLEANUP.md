# Cleanup Guide - Видалення глобальних файлів

Після переходу на локальне встановлення (`setup_local.sh`), можна видалити глобальні файли з першого встановлення.

## 🤖 Автоматичне видалення

```bash
chmod +x cleanup_global.sh
./cleanup_global.sh
```

Скрипт видалить:
- ✅ Глобальні Python пакети
- ✅ Whisper cache (~/.cache/whisper/)
- ✅ pip cache

**НЕ чіпає** (потрібні для локальної версії):
- ❌ Homebrew
- ❌ portaudio
- ❌ ffmpeg

---

## 🔧 Ручне видалення

### 1. Видалити глобальні Python пакети

```bash
pip3 uninstall -y pynput pyaudio websockets openai-whisper torch numpy
```

### 2. Видалити Whisper cache

```bash
# Перевірити розмір
du -sh ~/.cache/whisper

# Видалити
rm -rf ~/.cache/whisper
```

### 3. Очистити pip cache (опційно)

```bash
pip3 cache purge
```

---

## ⚠️ НЕ видаляти

Ці компоненти **потрібні** для локальної версії:

```bash
# Homebrew залишається
brew list portaudio  # ✓ Keep
brew list ffmpeg     # ✓ Keep

# Python сам залишається
which python3        # ✓ Keep
```

---

## 📊 Перевірка що залишилось

### Перевірити глобальні пакети

```bash
pip3 list | grep -E "(pynput|pyaudio|websockets|whisper|torch)"
```

Має бути **порожньо** після cleanup.

### Перевірити локальні пакети

```bash
source venv/bin/activate
pip list | grep -E "(pynput|pyaudio|websockets|whisper|torch)"
```

Має показати **встановлені** пакети в venv.

### Перевірити розмір проєкту

```bash
du -sh ~/work/openclaw-voice-hotkey
```

Очікуваний розмір: **~150-200MB** (з моделями)

- venv: ~50-80MB
- models/whisper: ~74MB
- models/tts: ~50MB
- bin/piper: ~5MB

---

## 🎯 Результат

Після cleanup:

✅ Усі залежності **локально** в проєкті  
✅ Глобальна система **чиста**  
✅ Можна видалити проєкт → все зникає  
✅ Можна клонувати в інше місце → все працює  

---

## 🔄 Повернутись до глобального (якщо треба)

```bash
# Видалити локальне
rm -rf venv/ models/ bin/

# Встановити глобально
./setup.sh
```
